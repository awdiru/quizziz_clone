# PS Quiz

**PS Quiz** — это веб-приложение для создания и проведения интерактивных викторин в реальном времени, разработанное как
функциональный аналог QUIZZIZ. Платформа позволяет учителям организовывать структуру тестов с помощью встроенной
файловой системы, проводить сессии в реальном времени и автоматически оценивать результаты учеников с учетом скорости их
ответов.

---

## Основной функционал

### 1. Управление контентом

* **Иерархическая файловая система:** Возможность создания директорий и поддиректорий для структурированного хранения
  тестов.
* **Конструктор тестов:** Создание вопросов с выбором нескольких вариантов ответов и указанием верных позиций.

### 2. Регистрация и безопасность

* **Модерируемая регистрация:** После заполнения формы регистрации учителем, администратор системы получает письмо на
  `ADMIN_EMAIL` со ссылкой-подтверждением. Доступ в систему возможен только после активации аккаунта администратором.
* **JWT-авторизация:** Защищенный вход для учителей.

### 3. Проведение тестирования (Real-time)

* **Игровые комнаты:** Учитель открывает комнату и получает уникальный **5-символьный код** доступа.
* **Подключение учеников:** Ученики входят в игру по коду и имени без необходимости регистрации.
* **Синхронный запуск:** Учитель полностью контролирует темп теста, переключая вопросы для всех участников одновременно.
* **Таблица лидеров:** Отображение прогресса учеников и их баллов в режиме реального времени на экране учителя.

### 4. Система оценки

Алгоритм начисления баллов учитывает как правильность, так и скорость ответа:

* **Идеальный ответ:** Если выбраны все верные ответы и нет лишних — баллы начисляются в зависимости от затраченного
  времени (чем быстрее, тем выше балл).
* **Частичный ответ / Ошибки:** Если выбраны не все верные ответы или добавлены лишние — итоговый балл пропорционально
  уменьшается.
* **Нулевой результат:** Если не выбрано ни одного верного ответа — 0 баллов.

---

## Деплой и запуск приложения

Для запуска проекта вам понадобится установленный **Docker** и **Docker Compose**.

### Шаг 1: Настройка окружения (Backend)

Создайте файл `.env` в корневом каталоге проекта и заполните его следующими переменными:

```env
DB_USERNAME="имя_пользователя_БД_или_email_администратора"
DB_PASSWORD="пароль_к_БД"

APP_JWT_SECRET="ваш_секретный_ключ_минимум_32_символа"

ALLOWED_ORIGINS=http://localhost:3000
APP_ADDRESS=http://localhost:3000
REACT_APP_API_URL=http://localhost:3000/api

ADMIN_EMAIL=admin@email.com
MAIL_HOST=smtp.yandex.ru
MAIL_USERNAME=org@yandex.ru
MAIL_PASSWORD="ваш_пароль_приложения"
MAIL_PROTOCOL=smtps
```

### Шаг 2: Настройка окружения (frontend)

Создайте файл .env в папке frontend:

```env
REACT_APP_API_URL=http://localhost:8080
```

### Шаг 3: Запуск через Docker

Откройте терминал в корневой папке проекта и выполните команду:

```bash
  docker-compose up --build
```

После завершения сборки приложение будет доступно по адресу:

* **Frontend:** http://localhost:3000
* **Backend API:** http://localhost:8080

---

## Технологический стек

### **Frontend:**

* React 19
* Create React App (CRA)
* React Router 7
* Tailwind CSS
* Lucide React
* Tailwindcss-animate
* QRCode.react
* Axios
* StompJS & SockJS
* Dotenv

#### 💡 Особенности реализации:

* Custom Design System: В tailwind.config.js определена кастомная палитра brand, включающая фирменные цвета (purple,
  yellow) и скругления rounded-quiz.
* Real-time Arena: Использование веб-сокетов позволяет учителю управлять ходом теста, а студентам видеть обновления без
  перезагрузки страницы.

### **Backend:**
* java 21
* Spring Boot 4.0.1
* Spring Security
* JSON Web Token (JWT)
* BCrypt
* PostgreSQL 17
* Spring Data JPA (Hibernate)
* Redis
* Spring Data Redis
* Spring WebSocket (STOMP)
* SpringDoc OpenAPI (Swagger)
* Docker & Docker Compose
* Spring Mail
* Lombok
* Jakarta Validation

```bash
docker exec certbot certbot certonly --webroot -w /var/www/certbot -d avdonin-evginiy-pc.ru -d www.avdonin-evginiy-pc.ru --email awdiru@gmail.com --agree-tos --no-eff-email
```